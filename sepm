Check port being listened

ss -tulnp | grep ":<port_number>"

=========================
restart sisips

 systemctl status sisips-init

systemctl start sisips-init

/opt/Symantec/sdcssagent/AMD/tools/sav manage -i /tmp/sylink.xml

status of comm with Manager
curl http://<SEPM_IP>:8014/secars/secars.dll?hello,secars

RE register ( need test)
sisipsconfig -host <manager_IP_or_hostname> -port 18444 -certfile /path/to/agent-cert.ssl -forcereg

openssl x509 -noout -text -in 'cerfile.cer'

================
lxpew22, lxpdir21, lxpew21, lxpntp22,lxpntp23,lxprep21, lxprep22,
/var/tmp/sep_ag_repo, sep_ag_rep.tar.gz

./LinuxInstaller  -- --local-repo file:/var/tmp/sep_ag_repo/SAL/1.4/


 ./LinuxInstaller  -- --proxy-mode Manual --proxy-address 10.42.240.220 --proxy-protocol HTTP --proxy-port 3128

==================================

On affected clients, run smc -stop, purge LiveUpdate settings, and restart with smc -start


Purge Live UPdate settings

1. smc -stop

2. C:\ProgramData\Symantec\LiveUpdate
Delete the following files (if present): 
	Settings.LiveUpdate
	LiveUpdate.conf
	Any .dat or .log files

C:\ProgramData\Symantec\Symantec Endpoint Protection\CurrentVersion\Data\Definitions
Delete all folders inside:
	VirusDefs
	IPSDefs
SonicDefs
	BehaviorDefs

3. smc -start

======================
SEPM Content Distribution

Confirm SEPM is downloading and distributing definitions correctly (Admin > Servers > Local Site > Show LiveUpdate Downloads)

Client Communication Logs

On affected agents, check SesmLu.log and Log.LiveUpdate Look for fallback attempts or policy mismatches
===============================
In SEPM, go to Reports > Quick Reports.

Choose Client Status or Virus Definitions Distribution.

Filter by group or time range to see which clients are up-to-date.
==========================
In SEPM:
Go to Admin > Servers > Local Site.
Click Show LiveUpdate Downloads to see the current definition version.

On the client:
Open SEP client UI.
Go to Help > Troubleshooting.
Check the Virus and Spyware Protection Definitions version.

If both match, the client is synced with SEPM.

